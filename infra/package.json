{
  "name": "nimkee-infra",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc",
    "deploy": "cdk deploy --all",
    "deploy:orders": "cdk deploy OrdersStack",
    "deploy:orders-direct": "cdk deploy OrdersDirectStack",
    "deploy:orders-api-direct": "cdk deploy OrdersApiDirectStack",
    "deploy:monolith": "cdk deploy MonolithStack",
    "diff": "cdk diff",
    "synth": "cdk synth",
    "bootstrap": "cdk bootstrap"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.180.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "aws-cdk": "^2.180.0",
    "typescript": "^5.7.3",
    "@types/node": "^22.10.7",
    "ts-node": "^10.9.2"
  }
}
